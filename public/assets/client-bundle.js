// watch the assigned client+bundle view
// hook up FROM(source) pod-based mangement events from the DB engine
// call INTO(destination) for the browserify command for each pod.  
// TODO: retry on the browser